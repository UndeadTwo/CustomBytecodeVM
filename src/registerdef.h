#ifndef _src_registerdef_h_
#define _src_registerdef_h_

typedef enum {
  REGA = 0x00,
  REGB = 0x01,
  REGC = 0x02,
  REGD = 0x03,
  REGCARRY = 0x04,
  REGPC = 0x05,
  REGACCUM = 0x06,
} registers;

#endif